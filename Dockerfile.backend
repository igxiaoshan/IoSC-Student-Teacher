# 后端 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装依赖
COPY backend/package*.json ./
RUN npm ci --only=production

# 复制所有源代码（node_modules 已安装）
COPY backend/ ./

# 创建上传目录
RUN mkdir -p uploads videos

# 暴露端口
EXPOSE 5000

# 启动命令
CMD ["node", "index.js"]
