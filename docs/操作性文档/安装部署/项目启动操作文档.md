# 学校管理系统 - 详细启动操作文档

## 目录
1. [环境准备](#一环境准备)
2. [数据库安装配置](#二数据库安装配置)
3. [项目安装](#三项目安装)
4. [环境变量配置](#四环境变量配置)
5. [启动项目](#五启动项目)
6. [常见问题](#六常见问题排查)
7. [生产部署](#七生产部署)

---

## 一、环境准备

### 1.1 必需软件清单

| 软件 | 版本要求 | 下载地址 | 用途 |
|------|---------|---------|------|
| Node.js | >= 16.0.0 | https://nodejs.org/ | 运行环境 |
| npm | >= 8.0.0 | 随Node.js安装 | 包管理 |
| MongoDB | >= 5.0 | https://www.mongodb.com/try/download/community | 数据库 |
| Git | 任意 | https://git-scm.com/ | 代码管理 |

### 1.2 Node.js 安装（Windows）

**步骤1：下载安装包**
- 访问 https://nodejs.org/
- 下载 LTS 版本（推荐 18.x 或 20.x）
- 运行下载的 `.msi` 文件

**步骤2：安装过程**
1. 双击安装包，点击 "Next"
2. 勾选 "I accept the terms in the License Agreement"，点击 "Next"
3. 选择安装路径（默认 `C:\Program Files\nodejs\`），点击 "Next"
4. 保持默认选项，点击 "Next"
5. 点击 "Install" 开始安装
6. 安装完成后点击 "Finish"

**步骤3：验证安装**
```bash
# 打开命令提示符（CMD）或 PowerShell
node -v
# 输出示例：v18.17.0

npm -v
# 输出示例：9.6.7
```

### 1.3 Node.js 安装（macOS）

```bash
# 使用 Homebrew 安装（推荐）
brew install node@18

# 验证安装
node -v
npm -v
```

### 1.4 配置 npm 镜像（加速下载）

```bash
# 设置淘宝镜像（国内推荐）
npm config set registry https://registry.npmmirror.com/

# 验证设置
npm config get registry
# 应输出：https://registry.npmmirror.com/

# 如需恢复官方镜像
npm config set registry https://registry.npmjs.org/
```

---

## 二、数据库安装配置

### 2.1 MongoDB 安装（Windows）

#### 方法1：使用 MSI 安装包（推荐）

**步骤1：下载**
- 访问 https://www.mongodb.com/try/download/community
- 选择：
  - Version: 7.0 (current) 或 6.0
  - Platform: Windows x64
  - Package: MSI
- 点击 "Download"

**步骤2：安装**
1. 双击下载的 `.msi` 文件
2. 点击 "Next"
3. 接受许可协议，点击 "Next"
4. 选择 "Complete"（完整安装），点击 "Next"
5. **重要**：勾选 "Install MongoDB as a Service"（将 MongoDB 安装为 Windows 服务）
6. 保持 "Run service as Network Service user"
7. 点击 "Next"
8. 勾选 "Install MongoDB Compass"（图形化管理工具，可选）
9. 点击 "Next"，然后 "Install"
10. 等待安装完成，点击 "Finish"

**步骤3：验证安装**
```bash
# 打开新的命令提示符
mongod --version
# 应输出类似：db version v7.0.0

# 检查 MongoDB 服务状态
sc query MongoDB
# 应显示 STATE: 4 RUNNING
```

#### 方法2：使用 ZIP 免安装版

```bash
# 1. 下载 ZIP 包并解压到 C:\mongodb
# 2. 创建数据目录
mkdir C:\data\db

# 3. 启动 MongoDB
C:\mongodb\bin\mongod.exe --dbpath C:\data\db

# 4. （可选）添加到环境变量 PATH: C:\mongodb\bin
```

### 2.2 MongoDB 安装（macOS）

```bash
# 使用 Homebrew 安装
brew tap mongodb/brew
brew install mongodb-community@7.0

# 启动 MongoDB 服务
brew services start mongodb-community@7.0

# 验证安装
mongod --version
```

### 2.3 MongoDB 基础操作

```bash
# 1. 启动 MongoDB Shell
mongosh

# 2. 查看所有数据库
show dbs

# 3. 切换到 school 数据库（项目使用的数据库）
use school

# 4. 查看当前数据库的集合（表）
show collections

# 5. 查看用户数据（如果已有数据）
db.students.find().limit(5)
db.teachers.find().limit(5)

# 6. 退出
exit
```

### 2.4 安装 MongoDB Compass（图形化工具）

**下载安装：**
- 访问 https://www.mongodb.com/products/compass
- 下载对应系统的安装包
- 安装并启动

**连接数据库：**
1. 打开 MongoDB Compass
2. New Connection
3. URI: `mongodb://localhost:27017`
4. 点击 "Connect"
5. 可以看到 `school` 数据库

---

## 三、项目安装

### 3.1 克隆项目

```bash
# 进入项目目录（用户已在该目录）
cd D:\core\code\IoSC-Student-Teacher-main

# 或者从 Git 克隆（如果是新项目）
# git clone <repository-url>
# cd IoSC-Student-Teacher-main
```

### 3.2 安装依赖

**方式1：分别安装（推荐）**

```bash
# 1. 安装根目录依赖
npm install

# 2. 安装后端依赖
cd backend
npm install
cd ..

# 3. 安装前端依赖
cd frontend
npm install
cd ..
```

**方式2：使用项目脚本**
```bash
# 项目可能提供了一键安装脚本
npm run setup-mirrors  # 设置国内镜像
npm install            # 安装根目录依赖
```

### 3.3 验证安装

```bash
# 检查各目录的 node_modules 是否存在
ls backend/node_modules    # Windows: dir backend\node_modules
ls frontend/node_modules   # Windows: dir frontend\node_modules
```

---

## 四、环境变量配置

### 4.1 后端配置 (`backend/.env`)

**步骤1：创建配置文件**

```bash
cd backend

# 复制示例配置
cp .env.example .env    # Linux/Mac
# 或 Windows: copy .env.example .env
```

**步骤2：编辑配置**

使用文本编辑器打开 `backend/.env`，根据需要进行配置：

```env
# ==========================================
# 数据库配置 (必需)
# ==========================================

# 本地 MongoDB (推荐开发使用)
MONGO_URL=mongodb://127.0.0.1/school

# 或 MongoDB Atlas (云数据库，生产推荐)
# MONGO_URL=mongodb+srv://用户名:密码@cluster0.xxx.mongodb.net/school?retryWrites=true&w=majority


# ==========================================
# 服务器配置 (必需)
# ==========================================

PORT=5000                              # 后端服务端口
NODE_ENV=development                   # 环境: development/production


# ==========================================
# AI 服务配置 (可选，用于 AI 功能)
# ==========================================

# Dify API 配置
DIFY_BASE_URL=https://api.dify.ai
DIFY_API_URL=https://api.dify.ai/v1
DIFY_API_KEY=your_api_key_here       # 替换为你的 Dify API Key

# Dify 应用 ID
DIFY_TEACHER_LESSON_APP_ID=your_app_id
DIFY_TEACHER_EXAM_APP_ID=your_app_id
DIFY_TEACHER_ANALYTICS_APP_ID=your_app_id
DIFY_STUDENT_LEARNING_APP_ID=your_app_id
DIFY_STUDENT_PRACTICE_APP_ID=your_app_id

# Ollama 本地 AI (可选)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=deepseek-r1


# ==========================================
# 安全与认证配置 (可选)
# ==========================================

JWT_SECRET=your_secret_key_here        # JWT 密钥
JWT_EXPIRES_IN=7d                      # Token 过期时间

# CORS 配置
CORS_ORIGIN=http://localhost:3000      # 前端地址


# ==========================================
# 文件上传配置 (可选)
# ==========================================

UPLOAD_PATH=./uploads                  # 上传文件路径
MAX_FILE_SIZE=10485760                 # 最大文件大小 (10MB)


# ==========================================
# Redis 缓存 (可选，用于高性能缓存)
# ==========================================

# REDIS_URL=redis://localhost:6379


# ==========================================
# 邮件配置 (可选，用于邮件通知)
# ==========================================

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_email_password
```

### 4.2 前端配置 (`frontend/.env`)

```bash
cd frontend

# 复制示例配置
cp .env.example .env    # Linux/Mac
# 或 Windows: copy .env.example .env
```

编辑 `frontend/.env`：

```env
# 后端 API 地址
REACT_APP_BASE_URL=http://localhost:5000
```

### 4.3 配置检查清单

- [ ] MongoDB 连接字符串正确
- [ ] 后端端口设置（默认 5000）
- [ ] 前端 API 地址指向正确后端地址
- [ ] AI 服务 API Key（如使用 AI 功能）
- [ ] JWT 密钥设置（生产环境必需）

---

## 五、启动项目

### 5.1 一键启动（推荐开发使用）

```bash
# 在项目根目录执行
npm run electron-dev
```

该命令会：
1. 启动后端服务（端口 5000）
2. 启动前端开发服务器（端口 3000）
3. 等待前端就绪后启动 Electron 桌面应用

### 5.2 分别启动（便于调试）

**步骤1：确保 MongoDB 运行**

```bash
# Windows - 检查服务状态
sc query MongoDB

# 如果未运行，启动服务
net start MongoDB

# 或使用 mongosh 连接验证
mongosh --eval "db.adminCommand('ping')"
```

**步骤2：启动后端**

```bash
cd backend
npm start
```

成功标志：
```
Server is running on port 5000
Connected to MongoDB successfully
```

**步骤3：启动前端**

```bash
cd frontend
npm start
```

成功后会自动打开浏览器，访问 `http://localhost:3000`

**步骤4：启动 Electron（可选）**

```bash
# 在项目根目录
npm run electron
```

### 5.3 仅 Web 模式（不使用桌面应用）

```bash
# 终端 1
cd backend && npm start

# 终端 2
cd frontend && npm start

# 访问 http://localhost:3000
```

### 5.4 启动验证

启动后验证各组件：

| 组件 | 验证方式 | 预期结果 |
|------|---------|---------|
| MongoDB | `mongosh --eval "db.adminCommand('ping')"` | `{"ok":1}` |
| 后端 API | 浏览器访问 `http://localhost:5000` | 显示 API 信息或文档 |
| 前端 | 浏览器访问 `http://localhost:3000` | 显示登录页面 |
| Electron | 桌面应用窗口 | 显示登录界面 |

---

## 六、常见问题排查

### 6.1 MongoDB 相关问题

**问题1：MongoDB 服务无法启动**
```bash
# 错误提示：MongoDB service is not running

# 解决方案：
# 1. 检查 MongoDB 是否安装为服务
sc query MongoDB

# 2. 手动启动服务
net start MongoDB

# 3. 如果服务不存在，手动创建
mongod --dbpath "C:\data\db" --logpath "C:\data\log\mongodb.log" --install

# 4. 再次启动
net start MongoDB
```

**问题2：连接 MongoDB 失败**
```bash
# 错误提示：MongoNetworkError: connection refused

# 解决方案：
# 1. 检查 MongoDB 是否运行
mongosh --eval "db.adminCommand('ping')"

# 2. 检查端口
netstat -an | findstr 27017

# 3. 检查防火墙设置，确保 27017 端口允许连接

# 4. 检查连接字符串
# 确认 backend/.env 中的 MONGO_URL 正确
# 本地: mongodb://127.0.0.1/school
```

**问题3：数据库权限错误**
```bash
# 错误提示：MongoAuthenticationError

# 解决方案：
# 1. 如果 MongoDB 启用了认证，修改连接字符串
MONGO_URL=mongodb://用户名:密码@127.0.0.1/school

# 2. 或关闭认证（仅开发环境）
# 编辑 MongoDB 配置文件 mongod.conf
# 注释掉 security: authorization: enabled
```

### 6.2 Node.js/npm 相关问题

**问题1：npm install 失败**
```bash
# 错误提示：ERR! code ECONNRESET 或网络错误

# 解决方案：
# 1. 设置国内镜像
npm config set registry https://registry.npmmirror.com/

# 2. 清除缓存后重试
npm cache clean --force
rm -rf node_modules package-lock.json
npm install

# 3. 使用 yarn 替代
npm install -g yarn
yarn install
```

**问题2：node-sass 编译失败**
```bash
# 错误提示：node-sass binding not found

# 解决方案：
# 1. 安装 windows-build-tools（Windows）
npm install --global windows-build-tools

# 2. 或切换 node 版本
nvm install 16
nvm use 16

# 3. 重新安装
rm -rf node_modules
npm install
```

**问题3：权限错误（EACCES）**
```bash
# 错误提示：EACCES: permission denied

# 解决方案（macOS/Linux）：
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) /usr/local/lib/node_modules

# Windows：以管理员身份运行命令提示符
```

### 6.3 项目启动相关问题

**问题1：端口被占用**
```bash
# 错误提示：EADDRINUSE: Port 5000/3000 is already in use

# 解决方案：
# 1. Windows - 查找并结束进程
netstat -ano | findstr :5000
taskkill /PID <进程ID> /F

# 2. macOS/Linux
lsof -i :5000
kill -9 <进程ID>

# 3. 或修改端口
# 编辑 backend/.env
PORT=5001

# 编辑 frontend/.env
REACT_APP_BASE_URL=http://localhost:5001
```

**问题2：后端启动成功但前端无法连接**
```bash
# 症状：前端页面报错 "Cannot connect to server"

# 解决方案：
# 1. 检查后端是否运行
curl http://localhost:5000
# 或浏览器访问 http://localhost:5000

# 2. 检查前端环境变量
cat frontend/.env
# 确认 REACT_APP_BASE_URL=http://localhost:5000

# 3. 检查防火墙
# 确保端口 5000 和 3000 未被防火墙阻止

# 4. 重启前端
cd frontend
npm start
```

**问题3：Electron 白屏或无法启动**
```bash
# 症状：Electron 窗口打开但显示空白

# 解决方案：
# 1. 确保前后端都已启动
# 2. 检查 electron-main.js 中的 loadURL
cat electron-main.js | grep loadURL

# 3. 开发模式应指向 localhost:3000
# win.loadURL('http://localhost:3000');

# 4. 检查控制台错误
# 在 Electron 窗口按 Ctrl+Shift+I 打开开发者工具

# 5. 清除 Electron 缓存
rm -rf %APPDATA%/school-management-system
```

**问题4：CORS 跨域错误**
```bash
# 错误提示：Access to XMLHttpRequest has been blocked by CORS policy

# 解决方案：
# 1. 检查 backend/.env 中的 CORS_ORIGIN
CORS_ORIGIN=http://localhost:3000

# 2. 确保与前端实际地址一致
# 如果前端运行在其他端口，修改 CORS_ORIGIN

# 3. 重启后端服务
```

### 6.4 AI 功能相关问题

**问题1：AI 功能无法使用**
```bash
# 症状：点击 AI 功能无响应或报错

# 解决方案：
# 1. 检查 Dify 配置
cat backend/.env | grep DIFY

# 2. 确认 API Key 有效
# 访问 https://dify.ai 获取 API Key

# 3. 检查网络连接
curl https://api.dify.ai/v1/health

# 4. 如果使用 Ollama 本地模型
# 检查 Ollama 服务
curl http://localhost:11434/api/tags
```

---

## 七、生产部署

### 7.1 构建生产版本

```bash
# 1. 确保使用生产环境变量
# 编辑 backend/.env
NODE_ENV=production

# 2. 构建前端
npm run build:frontend
# 输出到 frontend/build 目录

# 3. 安装后端生产依赖
npm run build:backend

# 4. 完整构建
npm run build
```

### 7.2 部署到服务器

**使用 PM2 进程管理器：**

```bash
# 1. 安装 PM2
npm install -g pm2

# 2. 创建 PM2 配置文件 ecosystem.config.js
module.exports = {
  apps: [{
    name: 'school-api',
    script: './backend/server.js',
    env: {
      NODE_ENV: 'production',
      PORT: 5000,
      MONGO_URL: 'mongodb://localhost/school'
    },
    instances: 1,
    autorestart: true,
    max_memory_restart: '500M'
  }]
};

# 3. 启动服务
pm2 start ecosystem.config.js

# 4. 保存配置并设置开机启动
pm2 save
pm2 startup
```

### 7.3 使用 Docker 部署

**创建 Dockerfile：**

```dockerfile
# 后端 Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm ci --only=production

COPY backend/ .

EXPOSE 5000

CMD ["npm", "start"]
```

**docker-compose.yml：**

```yaml
version: '3.8'

services:
  mongodb:
    image: mongo:6
    container_name: school-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: ./backend
    container_name: school-api
    restart: always
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGO_URL=mongodb://mongodb:27017/school
      - PORT=5000
    depends_on:
      - mongodb

volumes:
  mongo_data:
```

**启动 Docker：**

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

---

## 八、附录

### 8.1 项目结构参考

```
IoSC-Student-Teacher-main/
├── backend/                 # 后端 API
│   ├── controllers/         # 控制器
│   ├── models/             # 数据模型
│   ├── routes/             # 路由
│   ├── middleware/         # 中间件
│   ├── config/             # 配置文件
│   ├── .env                # 环境变量（需创建）
│   ├── .env.example        # 环境变量示例
│   └── server.js           # 入口文件
├── frontend/                # 前端 React 应用
│   ├── src/
│   │   ├── pages/          # 页面组件
│   │   ├── redux/          # 状态管理
│   │   ├── components/     # 公共组件
│   │   └── utils/          # 工具函数
│   ├── public/             # 静态资源
│   ├── .env                # 环境变量（需创建）
│   ├── .env.example        # 环境变量示例
│   └── package.json
├── electron-main.js         # Electron 主进程
├── preload.js              # Electron 预加载脚本
├── package.json            # 根目录配置
└── CLAUDE.md               # 项目说明
```

### 8.2 常用命令速查

```bash
# 开发启动
npm run electron-dev          # 一键启动开发环境
npm run start:backend         # 仅启动后端
npm run start:frontend        # 仅启动前端
npm run electron              # 仅启动 Electron

# 构建
npm run build                 # 完整构建
npm run build:frontend        # 仅构建前端
npm run build:backend         # 仅构建后端

# 打包
npm run dist:win              # Windows 安装包
npm run dist:mac              # macOS 安装包
npm run dist:linux            # Linux 安装包
npm run build-nosign          # 免签名打包

# 其他
npm run clean                 # 清理构建文件
npm run setup-mirrors         # 设置国内镜像
```

### 8.3 默认端口说明

| 服务 | 端口 | 配置位置 | 说明 |
|------|------|---------|------|
| MongoDB | 27017 | 安装配置 | 数据库服务 |
| 后端 API | 5000 | backend/.env | REST API 服务 |
| 前端开发服务器 | 3000 | 前端配置 | React 开发服务器 |
| Electron | - | - | 桌面应用（无端口） |

---

**文档版本：** v1.0
**最后更新：** 2026-04-13
**适用于：** School Management System v1.0.0

如需进一步帮助，请查看项目 `CLAUDE.md` 文件或联系开发团队。
