# 即梦AI 使用指南

## 概述

本系统集成了字节跳动火山引擎的即梦AI服务，支持**文生图**和**文生视频**功能。

---

## 功能介绍

### 1. 文生图 (即梦4.0)

基于先进的AI图像生成模型，根据文本描述生成精美图片。

**支持参数：**
| 参数 | 说明 | 可选值 |
|------|------|--------|
| width/height | 图片尺寸 | 多种比例可选 |
| scale | 文本影响度 | 0.1 - 1.0 |
| seed | 随机种子 | -1(随机) 或正整数 |

**图片比例：**
- 1:1 (正方形) - 1024×1024
- 16:9 (横版) - 1280×720
- 9:16 (竖版) - 720×1280
- 3:2 (宽版) - 1152×768
- 2:3 (竖长) - 768×1152

### 2. 文生视频 (即梦3.0)

专业级视频生成引擎，支持720P和1080P高清视频。

**支持参数：**
| 参数 | 说明 | 可选值 |
|------|------|--------|
| resolution | 视频质量 | 720p / 1080p |
| duration | 视频时长 | 5秒 / 10秒 |
| aspect_ratio | 视频比例 | 16:9, 4:3, 1:1, 3:4, 9:16, 21:9 |

**视频质量：**
- 720P 高清 - 推荐性价比之选
- 1080P 全高清 - 更清晰

---

## 环境配置

### 1. 获取API密钥

1. 访问 [火山引擎控制台](https://console.volcengine.com/)
2. 注册/登录账号
3. 开通"视觉生成"服务
4. 获取 Access Key 和 Secret Key

### 2. 配置环境变量

在 `backend/.env` 文件中添加以下配置：

```bash
# 即梦AI配置 (火山引擎视觉服务)
# API地址
JIMENG_API_URL=https://visual.volcengineapi.com

# 火山引擎访问密钥 (从控制台获取)
VOLC_ACCESS_KEY=your_access_key_here
VOLC_SECRET_KEY=your_secret_key_here

# 功能开关
JIMENG_IMAGE_ENABLED=true
JIMENG_VIDEO_ENABLED=true
```

### 3. 环境变量说明

| 变量名 | 说明 | 必填 |
|--------|------|------|
| JIMENG_API_URL | API服务地址 | 否（使用默认值） |
| VOLC_ACCESS_KEY | 访问密钥ID | **是** |
| VOLC_SECRET_KEY | 访问密钥密匙 | **是** |
| JIMENG_IMAGE_ENABLED | 开启文生图功能 | 否（默认开启） |
| JIMENG_VIDEO_ENABLED | 开启文生视频功能 | 否（默认开启） |

---

## 使用方式

### 教师端

1. 登录教师账号
2. 在侧边栏找到 **AI工具** 分区
3. 点击 **AI文生图** 或 **AI文生视频**
4. 输入描述文本，选择参数
5. 点击生成按钮

### 学生端

1. 登录学生账号
2. 在侧边栏找到 **AI助手** 分区
3. 点击 **AI文生图** 或 **AI文生视频**
4. 输入描述文本，选择参数
5. 点击生成按钮

---

## 功能特点

- **文生图**：
  - 支持多种图片比例
  - 可调节文本影响度
  - 历史记录保存
  - 一键下载图片

- **文生视频**：
  - 720P/1080P双版本
  - 5秒/10秒时长可选
  - 多种视频比例
  - 全屏播放支持

---

## 注意事项

1. **视频URL有效期**：生成的视频URL有效期为1小时，请及时下载
2. **生成时间**：视频生成通常需要1-3分钟，请耐心等待
3. **内容审核**：系统会自动进行内容审核，敏感词可能导致生成失败
4. **并发限制**：API有并发和QPS限制，如遇限流请稍后重试

---

## 错误码说明

| 错误码 | 说明 | 处理方式 |
|--------|------|----------|
| 10000 | 请求成功 | - |
| 50411 | 输入图片前审核未通过 | 检查输入内容 |
| 50412 | 输入文本审核未通过 | 检查描述文本 |
| 50413 | 文本含敏感词 | 修改描述后重试 |
| 50516 | 输出视频审核未通过 | 可重试 |
| 50429 | QPS超限 | 稍后重试 |
| 50430 | 并发超限 | 稍后重试 |
| 50500 | 内部错误 | 联系技术支持 |

---

## 开发说明

### 后端架构

```
backend/
├── config/jimengConfig.js    # 配置文件
├── services/jimengService.js # 服务封装
├── controllers/jimengController.js # 控制器
└── routes/jimeng.js         # 路由定义
```

### API接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/jimeng/text-to-image` | 文生图 |
| POST | `/api/jimeng/text-to-video` | 文生视频 |
| GET | `/api/jimeng/task/:taskId` | 查询任务状态 |
| GET | `/api/jimeng/models` | 获取模型列表 |
| GET | `/api/jimeng/history/:userId` | 获取历史记录 |

### 模拟模式

如果未配置API密钥（VOLC_ACCESS_KEY和VOLC_SECRET_KEY为空），系统会自动进入模拟模式，返回模拟数据用于开发测试。

---

## 技术支持

- 火山引擎文档：https://www.volcengine.com/docs/85621/1616429
- API错误码：参考火山引擎官方文档
